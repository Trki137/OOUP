package hr.fer.zemris.ooup;

import java.util.Objects;

public class Util {

    /*public static boolean checkIsValid(String content) {
        boolean isNumber = pattern.matcher(content).matches();

        if(isNumber) return true;

        if(!content.contains("+")) return false;

        String[] operators = content.split("\\+");

        for(String operator: operators)
            if(Objects.isNull(cell(operator.trim()))) return false;

        return true;
    }*/
}
